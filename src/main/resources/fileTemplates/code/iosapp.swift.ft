import ComposeApp
import SwiftUI

@main
struct iOSApp: App {
<#if IS_KOIN_ENABLE>
    init() {
        AppModuleKt.doInitKoin()
    }
</#if>
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
